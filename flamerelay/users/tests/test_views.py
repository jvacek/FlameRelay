from __future__ import annotations

# UserUpdateView was removed during the SPA migration — profile updates
# are now handled by PATCH /api/users/{username}/ via UserForm.tsx.
# API-level tests live in backend/tests/.
